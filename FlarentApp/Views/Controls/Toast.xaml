<UserControl
    x:Class="FlarentApp.Views.Controls.Toast"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlarentApp.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Storyboard x:Name="Notification" >
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="NotificationGrid"
                                           Storyboard.TargetProperty="Opacity" BeginTime="0:0:0">
                <SplineDoubleKeyFrame KeyTime="0:0:0.0" Value="1"/>
                <SplineDoubleKeyFrame KeyTime="0:0:1" Value="0.0"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </UserControl.Resources>
    <Grid Name="NotificationGrid">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,80" Padding="12,5" Background="#4D808080" CornerRadius="4" >
            <TextBlock Name="NotificationContent" TextWrapping="Wrap"  FontSize="18"/>
        </Border>
    </Grid>
</UserControl>
